{
  "name": "unicly-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --debug --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ 0xleia/unicly",
    "create-local": "graph create --node http://localhost:8020/ 0xleia/unicly",
    "remove-local": "graph remove --node http://localhost:8020/ 0xleia/unicly",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 0xleia/unicly"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0",
    "keytar": "^7.3.0"
  }
}
